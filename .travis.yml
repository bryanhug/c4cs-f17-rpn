language: python
python:
  - 3.6
  - nightly
# command to install dependencies
install:
  - pip install .
  - pip install -r requirements.txt
# command to run tests
script: make
repo_token: NLIB3EPimU8GwgpKi5TRo5XDfsYybTiXp
